doctype html
html
  head
    title="SVG SPRITE GENERATOR"
    link(rel="icon",type="image/png",href="#{virtualDirPath}/favicon.png")
    if(isProd)
        link(rel='stylesheet', href='#{virtualDirPath}/css/main.min.css')
    else
        link(rel='stylesheet', href='#{virtualDirPath}/css/main.css')
  body
    block content

    script(src='#{virtualDirPath}/scripts/jquery-3.2.0.min.js')
    if(isProd)
        script(src='#{virtualDirPath}/scripts/main.min.js')
    else
        script(src='#{virtualDirPath}/scripts/techlabs-frmk.js')
        script(src='#{virtualDirPath}/scripts/acc-web-generator.js')
    script
        | WebGenerator.init('#{virtualDirPath}');
    block scripts